export default async function InvoicesTable() {
    return (
        <div className="rounded-md bg-gray-50 p-4 text-sm text-gray-600">
            Starter-template invoice tables are not used in the Soki study app.
        </div>
    );
}
