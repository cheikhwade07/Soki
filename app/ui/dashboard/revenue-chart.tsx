import { lusitana } from '@/app/ui/fonts';

export default async function RevenueChart() {
    return (
        <div className="w-full rounded-xl bg-gray-50 p-6 shadow-sm">
            <h2 className={`${lusitana.className} mb-2 text-xl md:text-2xl`}>
                Placeholder
            </h2>
            <p className="text-sm text-gray-600">
                The revenue chart belonged to the starter template and is not part
                of the Soki learning workflow.
            </p>
        </div>
    );
}
